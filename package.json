{
  "name": "mama-minds",
  "version": "1.0.0",
  "description": "Postnatal mental health support app for African mothers — based on Edinburgh EPDS scale",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  },
  "keywords": ["ppd", "postpartum", "mental-health", "africa", "epds"]
}
